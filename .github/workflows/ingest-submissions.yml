name: Ingest Community Submissions

on:
  schedule:
    # Run every 6 hours
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  ingest:
    # Never run in mirror/fork repos (e.g. paddyspeaks-backup) — no secrets there
    if: github.repository == 'paddyriyer/paddyspeaks'
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'

      - run: pip install anthropic

      - name: Ingest community submissions
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GSHEET_CSV_URL: "https://docs.google.com/spreadsheets/d/e/2PACX-1vQR6l6cmujYC5HRzDwQAoz2OYhjhBber7xVmR5_J6ZMhW14nUpV126DM4Vu2-MEgNZdmgX7aI6iEaAC/pub?gid=399863191&single=true&output=csv"
        run: python .github/scripts/ingest_submissions.py

      # Pool sizes are baked into the Skill Check's title, meta description and
      # schema.org block. Without this the counts drift every single run.
      - name: Refresh hardcoded counts
        run: python3 interview/scripts/update_counts.py

      # The registry (data/site-registry.json) and every data-ps-stat span are
      # derived from the same data; regenerate them in the same commit so the
      # next PR's platform check never inherits drift from a bot run.
      - name: Refresh platform registry
        run: python3 scripts/platform_build/build.py registry stamp

      - name: Commit and push
        run: |
          git config user.name "PaddySpeaks Bot"
          git config user.email "bot@paddyspeaks.com"
          git add interview.app/evaluate/data/ interview.app/evaluate/index.html interview.app/evaluate/quiz.html .github/scripts/processed_submissions.json
          # The count refreshes above also touch the registry, the manifest
          # and stamped pages anywhere on the site.
          git add data/ interview/data/manifest.json
          git diff --name-only -z -- '*.html' | xargs -0 -r git add --
          git diff --cached --quiet && echo "No new questions" && exit 0
          git commit -m "🌍 Add community-submitted questions — $(date +%Y-%m-%d)"
          git push
